<UserControl x:Class="safari.Model.Views.LessonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:safari.Model.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="860">
    <Grid Background="GhostWhite" x:Name="ParentGrid1">
        <ContentControl x:Name="LoadHere">
            
        </ContentControl>

        <RadioButton Content="Open a Lesson"
                    x:Name="LessonButton1"
                    Height="70"
                    Foreground="Gray"
                    FontSize="16"
                    FontFamily="Reem Kufi"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Style="{StaticResource LightButtonTheme}" FontWeight="Bold"
                    Click="LessonButton_Click"
                    Width="210"/>
    </Grid>
</UserControl>
